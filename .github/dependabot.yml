version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: daily
      time: "06:00"
      timezone: Europe/Berlin
    open-pull-requests-limit: 10
    labels:
      - dependencies
    # Pin @types/node to the Node 24 LTS major. Dependabot will ship 24.x
    # patch/minor updates but never propose 25.x while we're on Node 24.
    ignore:
      - dependency-name: "@types/node"
        versions: [">=25"]
    groups:
      electron:
        patterns:
          - electron
          - electron-builder
      typescript:
        patterns:
          - typescript
          - typescript-eslint
          - "@typescript-eslint/*"
          - "@types/*"
      lint:
        patterns:
          - eslint
          - "@eslint/*"
      test:
        patterns:
          - vitest
          - "@vitest/*"
      build:
        patterns:
          - esbuild
          - tsx
      assets:
        patterns:
          - sharp
          - png-to-ico
      production-deps:
        dependency-type: production
      dev-deps:
        dependency-type: development
        exclude-patterns:
          - electron
          - electron-builder
          - typescript
          - typescript-eslint
          - "@typescript-eslint/*"
          - "@types/*"
          - eslint
          - "@eslint/*"
          - vitest
          - "@vitest/*"
          - esbuild
          - tsx
          - sharp
          - png-to-ico

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: daily
      time: "06:00"
      timezone: Europe/Berlin
    open-pull-requests-limit: 5
    labels:
      - dependencies
      - ci
    groups:
      actions:
        patterns:
          - "*"
